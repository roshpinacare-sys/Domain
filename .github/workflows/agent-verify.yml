# AGENT-VERIFY - הרשת מאמתת את הסוכן (Task 6)
#
# סוכן האימות העצמאי של הרשת: מודד את האתר החי מול assertions.json
# ומפרסם את התוצאות בפומבי. "שום טענה בלי מדידה" - גם לא טענות של
# הסוכן עצמו. אפס סודות: קריאה בלבד מה-URL הציבורי, כתיבה יחידה של
# agent/verify/results.json בחזרה לריפו. כישלונות מתפרסמים כמו שהם.

name: agent-verify

on:
  schedule:
    - cron: '55 */2 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: agent-verify
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run the verifier against the live site
        run: node agent/verify/run.mjs

      - name: Commit results if changed
        run: |
          if [ ! -f agent/verify/results.json ]; then
            echo "results.json missing - the verifier wrote nothing; nothing to commit"
            exit 0
          fi
          git config user.name "weave-verify[bot]"
          git config user.email "weave-verify[bot]@users.noreply.github.com"
          for attempt in 1 2 3; do
            git add agent/verify/results.json
            if git diff --cached --quiet -- agent/verify/results.json; then
              echo "results unchanged - no commit (attempt ${attempt})"
              exit 0
            fi
            VERDICT="$(node -p "require('./agent/verify/results.json').summary.verdict")"
            PASSED="$(node -p "require('./agent/verify/results.json').summary.passed")"
            TOTAL="$(node -p "require('./agent/verify/results.json').summary.total")"
            SUSP="$(node -p "require('./agent/verify/results.json').summary.suspended || 0")"
            git commit -m "agent-verify: ${VERDICT} · ${PASSED}/${TOTAL} · ${SUSP} suspended · $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            git pull --rebase origin main
            if git push; then
              echo "results committed on attempt ${attempt}: ${VERDICT} ${PASSED}/${TOTAL}"
              exit 0
            fi
            echo "push failed (attempt ${attempt}); resetting to origin and re-running the verifier"
            git checkout -- .
            git reset --hard origin/main
            node agent/verify/run.mjs
          done
          echo "ERROR: could not commit the results after 3 attempts"
          exit 1
